Link to completed project 
https://codepen.io/bleuxgenius/pen/VwwMaPz

## Answer to the review questions 

.   1. What is Semantic HTML?

        Introduces meaning to the web rather than just presentation. 
        i.e <p> indicates that the enclosed text is a paragraph. both semantical and presentational 
        because people know what paragraphs are, and browsers know how to display them.

    2. What is HTML used for? 
    
    HTML is short for Hyper Text Markup Language. 
    Its used to create electronic documents that are displayed on the world wide web . 
   
    3. What is an attribute and where do we put it? 
    
    an example of an attribute is :
    
    HTML links defined with a tag <a>,  the link address is specified with the (href) "attribute"
    
    <a href="https://www.google.com"
    
    4. What is the h1 tag used for? How many times should I use it on a page?
    
    is usually used for for the title of a page or post and it is the first header visible on a page. 
    
    you should only need one or two or three if you are keeping page consise. 
    
    5. Name two tags that have required attributes
    
    the link and the img tag 
    
    6. What do we put in the head of our HTML document? 
    
    <!Doctype html>
    <html>
    <head>
  
    
    7. What is an id? 
    
    the id attribute specifies a unique id for an HTML element
    
    8. What elements can I add an id to? 
    
    the identifier is used to specify the document. Used by CSS and javascript
    
    9. How many times can I use the same id on a page? 
    
    you can include an id multiple times as long as its valid. 
    
    10. What is a class? 
    
    class is an attribute which specifies one or more class names for an HTML element. The class attribute can be used on any HTML
      element. 
    
    11. What elements can I add a class to? 
    
    17 elements
    
    12. How many times can I use the same class on a page? 
    
    you're only limited by the maximum length of an X(HTML attribute)
    
    13. How do I get my link to open in a new tab?
    
    add target="_blank" attribute to your link (anchor tags)
    
    i.e: 
    
    <a href="https://www.thesitewizard.com/" target="_blank">thesitewizard.com</a>
  
    14. What is the alt attribute used for? 
    
    specifies an alternate text for an area, if the image cannot be displayed. The alt attribute
    provides alternative information for an image if a user for some reason cannot view it (because of slow connection)
    
    15. How do I reference an id?
    
    the id attribute can be used on any HTMNL element. , The id value is case-sensitive. Must not contain whitespace
    (spaces, tabs ect.)
    
    16. What is the difference between a section and a div?
    
    Section tags define sections in a document. such as chapters, headers, footers, or any other sections 
    of the document.
    Div tags is used to group block elements to format them with CSS.
  
    17. What is CSS used for? 
    
    CSS is the language for describing the presentation of web pages, including colors, layout, and fonts. It alloss one to 
    adapt the presentation to different types of devices. 
    
    18. How to we select an element? Example - every h2 on the page
    
    .h2{
    }
    
    19. What is the difference between a class and an id? - Give me an example of when I might use each one
    
    in CSS a class selector is .className
    in CSS an ID selector is #className
    
    difference between is an ID can be used to identify one element, and a class can identify multiple elements. 
    
    20. How do we select classes in CSS?
    
    .classSelected
    
    21. How do we select a p element with a single class of “human””?
    
    .class .p{
    }
    
    22. What is a parent child selector? When would this be useful? 
    
    element > element 
    
    or .class .element {
    }
    
    23. How do you select all links within a div with the class of sidebar?
    
    div class="sidebar bar-block" style="width:25%>
    
    
    24. What is a pseudo selector?
    
    used to define the special state of an element, can be combined with a CSS selector to add an effect to 
    exsiting elements based on their states.
    
    i.e changing the style of an element when the user hovers over it or when a link is visited. 
    
    25. What do we use the change the spacing between lines?
    
    line-height value. 
    
    26. What do we use to change the spacing between letters?
    
    letter-spacing: property
    
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?
    
    text-transform: capitilize;
    text-transform": lowercase;
    text-transform: uppercase;
    
    28. How do I add a 1px border around my div that is dotted and black?
    
    .div{
    border: 1px dotted;
    }
    
    29. How do I select everything on the page? 
    
    *{
    }
    
    30. How do I write a comment in CSS?
    cmd + //
    
    31. How do I find out what file I am in, when I am using the command line? 
    
    pwd 
    
    pwd = print working directory
    
    directory means = folder
    
    32. Using the command line - how do I see a list of files/folders in my current folder?
    
    ls
    
    33. How do I remove a file via the command line? Why do I have to be careful with this? 
    
    -r
    recursive option. to tell the terminal to remove/delete the folder. Can not undo this function.
    
    34. Why should I use version control? 
    
    helps teams solve these kinds of problems, tracking every individual change by each contributor and helping prevent 
    concurrent work from conflicting. 
    
    35. How often should I commit to github?
    
    commit often , perfect later, publish once.
    
    36. What is the command we would use to push our repo up to github? 
    
    git push
    
    37. Walk me through Lambda's git flow. 
    
    commit (often)
    push
    create pull request
    

Stretch Questions

    1. What is the difference between an inline element and a block element?
    
    A block-level element always starts on a new line and takes up the full width available 
    (stretches out to the left and right as far as it can).
    
    An inline element does not start on a new line and only takes up as much width as necessary.
    
    2. What happens when an element is positioned absolutely? 
    
    An element with position:absolute is removed from the normal document flow. psoitioned automatically 
    to the starting-point (top-left-corner) of its parent element.
    
    3. How do I make an element take up only the amount of space it needs 
    but also have the ability to give it a width? 
    
  the CSS block model 
  
  margin , display types, padding, and width .
    
    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default 
    and 1 element that is display inline-block by default
    
    display-block :
    
    <div>
    <h1>-<h6>
    <p>
    <form>
    <header>
    <footer>
    <section>
    
    inline- display 
    
    
    inline-block default - 
 <span>
<a>
<img>
    
    5. In your own words, explain the box model. What is the fix for the box model? 

In CSS, the term "box model" is used when talking about design and layout. 
The CSS box model is essentially a box that wraps around every HTML element. 
It consists of: margins, borders, padding, and the actual content.

   --> 
